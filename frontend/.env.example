# Base URL for the FastAPI backend API (e.g. http://localhost:8000)
# The frontend will also automatically derive the WebSocket (ws:// or wss://) URL from this base.
VITE_API_BASE_URL=http://localhost:8000
